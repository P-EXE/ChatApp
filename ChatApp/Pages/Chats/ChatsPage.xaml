<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ChatApp.ViewModels"
             xmlns:v="clr-namespace:ChatApp.Views"
             x:Class="ChatApp.Pages.ChatsPage"
             x:DataType="vm:ChatsVM">
  <Shell.TitleView>
    <Button Text="Refresh" Command="{Binding RefreshChatListCommand}"/>
  </Shell.TitleView>
  <Grid RowDefinitions="*,auto">
    <RefreshView Grid.Row="0" Command="{Binding RefreshChatListCommand}" IsRefreshing="{Binding ChatList_isRefreshing}">
      <CollectionView SelectionMode="Single"
                      ItemsSource="{Binding Chats}"
                      SelectedItem="{Binding SelectedChat}"
                      SelectionChangedCommand="{Binding NavToChatCommand}">
        <CollectionView.ItemTemplate>
          <DataTemplate>
            <SwipeView>
              <v:ChatListV/>
            </SwipeView>
          </DataTemplate>
        </CollectionView.ItemTemplate>
      </CollectionView>
    </RefreshView>
    <Button Grid.Row="1" Text="New Chat" Command="{Binding CreateNewChatCommand}"/>
  </Grid>
</ContentPage>